#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include "auronacci.h"

enum class frequ;

enum class trigon;

enum class movement;

enum class dimension;

enum class coloring;

unsigned char unchar(float input);

float frequify_all(auronacci &gold);

#endif // FUNCTIONS_H
